import Range = require("../../../../frontend/frontend/node_modules/@types/semver/classes/range");
import semver = require("../../../../frontend/frontend/node_modules/@types/semver/index");

/**
 * Mostly just for testing and legacy API reasons
 */
declare function toComparators(range: string | Range, optionsOrLoose?: boolean | semver.Options): string[][];

export = toComparators;
